package concrete.goonie.indicators;

public enum ENUM_PRICE_METHOD {
    SIMPLE,       // Simple Moving Average (SMA)
    EXPONENTIAL,  // Exponential Moving Average (EMA)
    SMOOTHED,     // Smoothed Moving Average (SMMA)
    LINEARWEIGHTED // Linear Weighted Moving Average (LWMA)

}
